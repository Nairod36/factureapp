{
  "name": "facture-usdc-contracts",
  "version": "1.0.0",
  "description": "Smart contracts pour l'application Facture USDC",
  "main": "web3_service.js",
  "scripts": {
    "compile": "node compile.js",
    "deploy": "npm run compile && node deploy/deploy.js",
    "test": "mocha test/*.test.js",
    "deploy:sepolia": "npm run compile && NETWORK=sepolia node deploy/deploy.js",
    "deploy:polygon": "npm run compile && NETWORK=polygon node deploy/deploy.js"
  },
  "dependencies": {
    "ethers": "^6.7.1",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "mocha": "^10.2.0",
    "chai": "^4.3.7",
    "solc": "^0.8.20"
  },
  "keywords": ["ethereum", "solidity", "usdc", "invoices", "smart-contracts"],
  "author": "Votre nom",
  "license": "MIT"
}
